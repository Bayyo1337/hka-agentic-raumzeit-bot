Rolle: Python Backend Developer
Aufgabe: Behebe den Fehler in der Studiengangs-Zuordnung in 'src/tools.py'.

Kontext:
Der Bot erkennt aktuell nur Kürzel (z.B. 'MABB'), aber keine Klarnamen wie 'Maschinenbau'.
In 'src/tools.py' gibt es die Funktion 'resolve_course_name(query: str)'. 
Dort muss die zweite Schleife implementiert werden, die den 'longName' der Studiengänge prüft.

Schritte:
1. Analysiere 'src/tools.py', insbesondere 'resolve_course_name'.
2. Implementiere den Teil-Match auf den 'longName', wie in '.gemini_agents/prompts/problem.md' unter 'Umsetzungsschritte' beschrieben.
3. Verifiziere die Änderung (z.B. durch ein kleines Test-Skript oder manuellen Aufruf der Funktion).
4. Protokolliere deinen Erfolg und die vorgenommenen Änderungen am Ende von '.gemini_agents/session_log.md'.

Referenz:
- Analyse: .gemini_agents/prompts/problem.md
- Ziel-Datei: src/tools.py

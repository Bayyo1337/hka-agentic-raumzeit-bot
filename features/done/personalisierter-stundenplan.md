togglepersonal ist noch nicht implementiert, dafür wird eine datenbank gebraucht, die für jeden telegram user, die kurse beinhaltet. Das auswählen soll interaktiv sein: man sagt man hat aus mabb 7 gastdozentur, dann ruft der bot das auf und fragt ob diese termine oder das modul richtig ist, dies kann dann vom user bestätigt werden und wird gespeichert. Auch soll die Möglichkeit bestehen dann den ganzen Stundenplan auszugeben, ruhig eine neue Nachricht pro Tag für alle 5 Tage oder manchmal ist der samstag auch dabei. Der samstag soll nur ausgegeben werden, wenn ein termin vorhanden! Auch soll der Stundenplan wenn einmal angelegt, zwischengespeichert werden um die ausgabe schneller zu machen ohne llm usw, einfach ein Befehl mit / und dann kommt der persönliche Stundenplan.